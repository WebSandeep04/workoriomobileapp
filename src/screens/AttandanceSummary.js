import { View, Text } from 'react-native';
import Header from '../components/Header';

const AttandanceSummary = () => {
    return (
        <View style={{ flex: 1, backgroundColor: '#fff' }}>
            <Header title="Attandance Summary" />

        </View>
    );
};

export default AttandanceSummary;
